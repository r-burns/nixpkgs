diff --git a/CMakeLists.txt b/CMakeLists.txt
index ca2b3af6fe..ba269b7864 100644
--- a/CMakeLists.txt
+++ b/CMakeLists.txt
@@ -581,7 +581,7 @@ else()
   add_subdirectory(Externals/enet)
 endif()
 
-if(NOT XXHASH_FOUND)
+if(FALSE)
   message(STATUS "Using static xxhash from Externals")
   add_subdirectory(Externals/xxhash)
 endif()
@@ -639,7 +639,7 @@ else()
   include_directories(External/minizip)
 endif()
 
-if(NOT APPLE)
+if(TRUE)
   check_lib(LZO "(no .pc for lzo2)" lzo2 lzo/lzo1x.h QUIET)
 endif()
 if(LZO_FOUND)
@@ -651,7 +651,7 @@ else()
   set(LZO lzo2)
 endif()
 
-if(NOT APPLE)
+if(TRUE)
   check_lib(PNG libpng png png.h QUIET)
 endif()
 if (PNG_FOUND)
@@ -690,10 +690,10 @@ endif()
 
 if(NOT ANDROID)
   add_definitions(-D__LIBUSB__)
-  if(NOT APPLE)
+  if(TRUE)
     find_package(LibUSB)
   endif()
-  if(LIBUSB_FOUND AND NOT APPLE)
+  if(LIBUSB_FOUND)
     message(STATUS "Using shared LibUSB")
     include_directories(${LIBUSB_INCLUDE_DIR})
   else()
@@ -706,7 +706,7 @@ if(NOT ANDROID)
 endif()
 
 set(SFML_REQD_VERSION 2.1)
-if(NOT APPLE)
+if(TRUE)
   find_package(SFML ${SFML_REQD_VERSION} COMPONENTS network system)
 endif()
 if(SFML_FOUND)
@@ -720,7 +720,7 @@ else()
 endif()
 
 if(USE_UPNP)
-  if(NOT APPLE)
+  if(TRUE)
     find_package(Miniupnpc)
   endif()
   if(MINIUPNPC_FOUND AND MINIUPNPC_API_VERSION GREATER 8)
@@ -733,7 +733,7 @@ if(USE_UPNP)
   add_definitions(-DUSE_UPNP)
 endif()
 
-if(NOT APPLE)
+if(TRUE)
   find_package(MbedTLS)
 endif()
 if(MBEDTLS_FOUND)
@@ -784,9 +784,9 @@ if(NOT ANDROID)
 endif()
 
 if(USE_DISCORD_PRESENCE)
-  message(STATUS "Using static DiscordRPC from Externals")
-  add_subdirectory(Externals/discord-rpc)
-  include_directories(Externals/discord-rpc/include)
+  #message(STATUS "Using static DiscordRPC from Externals")
+  #add_subdirectory(Externals/discord-rpc)
+  #include_directories(Externals/discord-rpc/include)
 endif()
 
 find_package(Libsystemd)
